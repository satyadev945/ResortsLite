apiVersion: v1
kind: Service
metadata:
  name: orcappdbmmono-service
  namespace: orcappdbmmono
  labels:
    app: orcappdbmmono
    app.kubernetes.io/name: orcappdbmmono
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: orcappdbmmono
