apiVersion: v1
kind: Service
metadata:
  name: resortslite-service
  namespace: resortslite
  labels:
    app: resortslite
    app.kubernetes.io/name: resortslite
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: resortslite
