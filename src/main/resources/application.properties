# ResortsLite - Cloud-Native Configuration
spring.application.name=ResortsLite

# Server port - externalized to environment variable for cloud deployment
server.port=${SERVER_PORT:8080}

# Database configuration - externalized to environment variables
spring.datasource.url=${DB_URL:jdbc:h2:mem:resortdb;DB_CLOSE_DELAY=-1}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}

# HikariCP connection pool configuration
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:5}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:1800000}

spring.h2.console.enabled=${H2_CONSOLE_ENABLED:false}
spring.h2.console.path=/h2-console

# Redis configuration for distributed session management (ElastiCache)
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.redis.ssl=${REDIS_SSL:false}
spring.redis.timeout=${REDIS_TIMEOUT:2000}

# Spring Session configuration
spring.session.store-type=redis
spring.session.redis.flush-mode=on_save
spring.session.redis.namespace=spring:session

# Redis cache configuration with TTL
spring.cache.type=redis
spring.cache.redis.time-to-live=${CACHE_TTL:3600000}
spring.cache.redis.cache-null-values=false

# AWS configuration - region from environment
aws.region=${AWS_REGION:us-east-1}

# AWS S3 configuration for file storage
aws.s3.bucket.name=${S3_BUCKET_NAME:resorts-lite-reports}
aws.s3.bucket.region=${AWS_REGION:us-east-1}

# AWS Secrets Manager configuration
aws.secrets.db.secret.name=${DB_SECRET_NAME:resorts-lite/db/credentials}
aws.secrets.auth.secret.name=${AUTH_SECRET_NAME:resorts-lite/auth/credentials}

# AWS Systems Manager Parameter Store configuration
aws.ssm.parameter.payment.endpoint=${PAYMENT_ENDPOINT_PARAM:/resorts-lite/endpoints/payment}
aws.ssm.parameter.inventory.endpoint=${INVENTORY_ENDPOINT_PARAM:/resorts-lite/endpoints/inventory}
aws.ssm.parameter.notification.endpoint=${NOTIFICATION_ENDPOINT_PARAM:/resorts-lite/endpoints/notification}
aws.ssm.parameter.report.url=${REPORT_URL_PARAM:/resorts-lite/endpoints/report}

# External service endpoints - externalized to environment variables
app.payment.endpoint=${PAYMENT_ENDPOINT:http://payment-svc:9090/charge}
app.inventory.endpoint=${INVENTORY_ENDPOINT:http://inventory-svc:8081/rooms}
app.notification.endpoint=${NOTIFICATION_ENDPOINT:http://notify-svc:7070/send}
app.report.base.url=${REPORT_BASE_URL:https://reports.resorts.com}

# File storage configuration
app.storage.type=${STORAGE_TYPE:s3}
app.storage.local.path=${LOCAL_STORAGE_PATH:/tmp/reports}

# JWT Configuration
jwt.secret=${JWT_SECRET:resortsLiteSecretKeyForJWTTokenGeneration2024}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:8080}
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*
cors.allow.credentials=true
